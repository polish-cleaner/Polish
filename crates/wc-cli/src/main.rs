//! `polish` CLI. v1.2 Pro tier feature per PROJECT.md §5 / PLAN.md.
//! Stub binary so the workspace compiles end-to-end.

fn main() -> anyhow::Result<()> {
    println!("polish CLI placeholder. Full CLI ships v1.2 Pro tier.");
    Ok(())
}
