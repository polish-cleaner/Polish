//! `wc-adapters` — concrete adapters implementing `wc-core` ports.
//!
//! Modules below are placeholders. Implementations land per ARCHITECTURE.md §2
//! once the Tauri+service IPC critical spike (PROJECT.md §11 Week 1–3) passes.

#![forbid(unsafe_code)]

pub mod categories;
// pub mod fs;
// pub mod registry;
// pub mod wsl;
// pub mod pq_writer;
// pub mod notification;
// pub mod signing;
